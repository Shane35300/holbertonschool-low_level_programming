#include "main.h"

/**
 * _isupper - isupper function
 * Description: check uppercase
 * Return: return to 1 or 0
 *@c: variable c
 */
int _isupper(int c)
{

	if ('A' <= c && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
