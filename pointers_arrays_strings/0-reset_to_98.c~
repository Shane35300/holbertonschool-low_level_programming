#include "main.h"

/**
 * reset_to_98 - check the code
 * Description : takes a pointer to an int as parameter
 * @n: pointer
 */
void reset_to_98(int *n)
{
	int *ptr = 98
	int *ptr = &n;

	updateValue(ptr);
}
