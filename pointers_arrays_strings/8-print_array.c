#include "main.h"
/**
 * print_array - print
 * Description : print reversed
 *@a: a pointer/
 *@n: integer
 */
void print_array(int *a, int n)
{
	for (nb = 0 ; nb <= n ; nb++)
	{
		printf("%d", a[nb]);
	}
	printf("\n");
}
