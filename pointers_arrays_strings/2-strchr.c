#include "main.h"
/**
* _strchr - fonction
* Description : locate a caracter in a string
* @s: string
* @c: caracter
* Return: return
*/
char *_strchr(char *s, char c)
{
	int i = 0;

	while (s[i] == '\0' || s[i] == 'c')
	{
		i++;
	}
	return (s);
}
